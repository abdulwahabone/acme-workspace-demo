{
  "name": "acme-workspace-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Test material: a three-app workspace (storefront, backoffice, docs).",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:storefront": "npm run build -w storefront",
    "dev:storefront": "npm run dev -w storefront",
    "start:backoffice": "npm start -w backoffice"
  }
}
